<tr>
    {{#if hasBlock}}
        {{yield header}}
    {{else}}
        {{#each columnKeys as |column| }}
            {{simple-table-cell header=header dataKey=column }}
        {{/each}}
    {{/if}}
</tr>
