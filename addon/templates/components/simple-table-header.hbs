{{#if hasBlock}}
    {{yield}}
{{else}}
    <tr>
        {{#each columns as |column| }}
            {{simple-table-cell data=headerRow dataKey=column sortBy=(action sortBy)}}
        {{/each}}
    </tr>
{{/if}}
