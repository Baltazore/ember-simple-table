<tr>
    {{#if hasBlock}}
        {{yield header}}
    {{else}}
        {{#each columns as |column| }}
            {{simple-table-cell data=headerRow dataKey=column sortBy=attrs.sortBy}}
        {{/each}}
    {{/if}}
</tr>
