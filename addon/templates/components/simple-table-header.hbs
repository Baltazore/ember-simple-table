<tr>
    {{#if hasBlock}}
        {{yield header}}
    {{else}}
        {{#each columns as |column| }}
            {{simple-table-cell header=header dataKey=column }}
        {{/each}}
    {{/if}}
</tr>
