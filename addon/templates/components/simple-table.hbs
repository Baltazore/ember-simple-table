<thead>
    {{simple-table-header rowData=headerRow keys=columns sortBy=(action 'sortBy')}}
</thead>

<tbody>
    {{#each bodyRows as |rowData| }}
        {{simple-table-row rowData=rowData keys=columns}}
    {{/each}}
</tbody>
