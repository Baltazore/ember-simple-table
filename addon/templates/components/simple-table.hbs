{{#if hasBlock}}
    {{yield (hash
      header=(component "simple-table-header" columns=tColumns sortAction=(action "sortBy"))
      headerRow=(component "simple-table-header-row" columns=tColumns sortAction=(action "sortBy"))
      body=(component "simple-table-body" columns=tColumns rows=tRows)
      rows=tRows
      )
    }}
{{else}}
    {{simple-table-header columns=tColumns sortAction=(action "sortBy")}}
    {{simple-table-body columns=tColumns rows=tRows}}
{{/if}}
